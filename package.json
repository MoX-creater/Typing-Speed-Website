{
  "name": "typing-speed-website-master",
  "version": "1.0.0",
  "description": "MERN Stack Typing Speed App",
  "scripts": {
    "start": "cd client && npm run dev",
    "server": "echo \"No server backend configured\"",
    "client": "cd client && npm run dev",
    "dev": "cd client && npm run dev",
    "install-all": "npm install && cd client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "firebase": "^12.12.1"
  }
}
